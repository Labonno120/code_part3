#include<iostream>
#include<bits/stdc++.h>
#define ll long long int
#define pi acos(-1)
using namespace std;
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n;
        cin>>n;
        string s1,s2;
        cin>>s1>>s2;
        for(ll i=0;i<n;i++)
        {
            if(s1[i]==s2[i])
                continue;
            else if(abs((s1[i]-'0')-(s2[i]-'0'))==1){


            }
        }
    }
}
